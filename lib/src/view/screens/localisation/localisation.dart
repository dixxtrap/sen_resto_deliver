import 'package:flutter/material.dart';

class Localisation extends StatelessWidget {
  const Localisation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Localisation');
  }
}
